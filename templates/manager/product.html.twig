{% extends 'base.html.twig' %}
{% block title %}Manager Product{% endblock %}
{% block body %}
    <h1>Product List</h1>
    <button style ="border-radius:10px;
    margin-bottom : 20px;
    background:rgb(0, 149,246,0.3);
    border:1px solid white;
    " ><a href ="{{path('addproduct')}}">Add Product</a></button>
    <table class="table">
        <thead>
            <tr>
                <th>No.</th>
                <th>ID Product</th>
                <th>Name</th>
                <th>Price</th>
                <th>Actions</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
         {% for p in pro %}
            <tr>
                <td>{{ p.id }}</td>
                <td>{{ p.idpro }}</td>
                <td>{{ p.namepro }}</td>
                <td>{{ p.price}}</td>
                <td>
                    <a href="{{ path('edit', { 'id': p.id }) }}">Edit</a>
                    
                </td>
                 <td>
                    <a href="{{ path('product_delete', { 'id': p.id }) }}"><i class="fa-solid fa-trash-can"></i></a>
                    
                </td>
               
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
